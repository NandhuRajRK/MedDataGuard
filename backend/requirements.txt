fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-multipart>=0.0.9

# Keep scientific stack unpinned so pip can choose wheels compatible with
# the local Python version (e.g., Python 3.13 on Windows).
numpy>=2.1
pandas>=2.2
opencv-python>=4.10
Pillow>=10.4
scikit-learn>=1.5
imagehash>=4.3

pytest>=8.3
ruff>=0.5
